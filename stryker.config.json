{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "testRunner": "vitest",
  "coverageAnalysis": "perTest",
  "tsconfigFile": "tsconfig.json",
  "reporters": ["html", "clear-text", "progress"],
  "htmlReporter": { "fileName": "reports/mutation/index.html" },
  "clearTextReporter": { "maxTestsToLog": 3 },
  "_comment": "Mutation testing measures whether the suite actually kills injected faults, not just whether lines run. Scoped to the correctness and security core; the lexer, parser, async evaluator, stdlib, and autocomplete are deliberately deferred to follow-up expansions. `break` is null until a baseline is recorded, then it should be ratcheted up like the coverage floor in vitest.config.ts.",
  "mutate": [
    "src/execution-context.ts",
    "src/eval-ops.ts",
    "src/safe-methods.ts",
    "src/coerce.ts",
    "src/compiler.ts",
    "src/evaluator.ts"
  ],
  "thresholds": { "high": 90, "low": 80, "break": 65 }
}
